<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen Theme untuk API 31+ (kompatibel dengan versioning system) -->
    <style name="Theme.FTxT.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/colorPrimary</item>
    </style>

</resources>
